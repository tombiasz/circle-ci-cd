version: 2.1
orbs:
  node: circleci/node@2.1.1
jobs:
  build-and-test:
    executor:
      name: node/default
    steps:
      - checkout
      - run: npm
      - run: npm run test:unit
      - run: npm run test:integration
workflows:
    build-and-test:
      jobs:
        - build-and-test
